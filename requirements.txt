# Streamlit Community Cloud — Python dependencies
# Keep versions flexible but conflict-free for Python 3.11–3.13

streamlit>=1.32.0,<2
python-dotenv>=1.0.0

# Notebook / markdown / highlighting / execution
nbformat>=5.10.0
nbclient>=0.10.0
jupyter_client>=8.6.0
ipykernel>=6.29.0
markdown>=3.5
Pygments>=2.17.0

# PDF engines (WeasyPrint preferred; xhtml2pdf + reportlab as fallbacks)
weasyprint>=62.0
xhtml2pdf>=0.2.15
reportlab>=4.1.0

# LangChain / LangGraph agent stack
langchain>=0.3.0
langchain-core>=0.3.0
langgraph>=0.2.0
langchain-openai>=0.2.0
langchain-groq>=0.2.0
langchain-google-genai>=2.0.0
langchain-anthropic>=0.2.0

# Official provider SDKs (API key validation)
openai>=1.40.0
groq>=0.9.0
google-genai>=1.0.0
anthropic>=0.34.0

# Image support for WeasyPrint (JPEG/PNG notebook outputs)
Pillow>=10.0.0
